@model IEnumerable<Calculator.Models.Operation>

<div class="result">
    <table>
        <thead>
            <tr>
                <td style=" border: 5px solid black">
                    Выражение = Результат
                </td>
            </tr>
        </thead>
        <tbody id="expressionsList">
            @foreach (var operation in Model)
            {
                <tr><td>@operation.Expression = @operation.Result</td></tr>
            }
        </tbody>

    </table>
</div>